syntax = "proto3";

option go_package = "github.com/AmanTrance/FastRpc/benchmarks/grpc_bench/proto";

package proto;

service EchoService {
  rpc Echo (Payload) returns (Payload) {}
}

message Payload {
  bytes data = 1;
}